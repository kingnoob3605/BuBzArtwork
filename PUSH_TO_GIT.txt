=============================================
  HOW TO PUSH YOUR GALLERY TO VERCEL
=============================================

1. OPEN TERMINAL (Git Bash or Command Prompt)
   - Right-click inside the WEB folder → "Open in Terminal"
   - OR open VS Code → drag the WEB folder in → open Terminal

2. STAGE ALL CHANGES
   git add .

3. COMMIT WITH A MESSAGE
   git commit -m "your update message here"

4. PUSH TO GITHUB
   git push origin main

5. VERCEL DEPLOYS AUTOMATICALLY
   - Go to https://vercel.com/dashboard
   - You should see a new deployment triggered
   - Wait ~30 seconds and visit https://bubz-artwork.vercel.app/

=============================================
  WHAT'S DONE (as of this session)
=============================================

[x] Supabase migration — all data now stored in cloud DB
    Tables: comments, reactions, wall_posts, wall_replies,
            poll_votes, pending_submissions, private_inbox,
            extra_artworks, art_overrides, hidden_arts

[x] Admin login — Supabase Auth (email + password)
    No more hardcoded password!

[x] Hardcoded email removed — email is typed at login

[x] Credentials secured — keys.js is gitignored
    (Supabase URL + anon key live in keys.js only)

[x] Multiple photo upload — add art supports batch URLs

[x] WigglyPaint fullscreen — ⛶ button in WigglyPaint toolbar

[x] Community Wall — Reply threads (Twitter-style flat replies)

[x] Community Wall — Poll feature
    - Admin creates polls from admin panel (📊 Poll tab)
    - Visitors vote, see live % bars
    - Double-vote prevention via localStorage token

[x] Wall filter — All / 🎨 Drawings / 💬 Messages / 📊 Polls

[x] Image upload — catbox.moe (free, no registration)
    - Switched from Imgur (no Client ID needed)
    - Upload from device button works out of the box

[x] Supabase MCP config — created at:
    C:\Users\Internship\.claude\mcp.json

=============================================
  NEXT STEPS — TODO
=============================================

[ ] IMGUR API (optional — if you prefer Imgur over catbox.moe)
    - Go to: https://api.imgur.com/oauth2/addclient
    - App name: BubzArtwork
    - Type: Anonymous usage without user authorization
    - Copy Client ID → paste into keys.js

[ ] SUPABASE MCP — finish setup
    - Restart Claude Code after creating mcp.json
    - Authenticate with Supabase in the browser when prompted
    - Then Claude can directly query/fix your DB for bugs

[ ] SUPABASE KEY ROTATION (IMPORTANT — security!)
    - Old anon key was committed to git (GitGuardian flagged it)
    - Go to: Supabase → Settings → API → Regenerate anon key
    - Update the new key in keys.js

[ ] SUPABASE SQL — run these tables if not done yet
    (open Supabase SQL Editor and run)

    CREATE TABLE IF NOT EXISTS wall_replies (
      id TEXT PRIMARY KEY,
      post_id TEXT NOT NULL,
      text TEXT NOT NULL,
      sender TEXT,
      timestamp TIMESTAMPTZ NOT NULL,
      created_at TIMESTAMPTZ DEFAULT now()
    );

    CREATE TABLE IF NOT EXISTS poll_votes (
      post_id TEXT NOT NULL,
      option_idx INTEGER NOT NULL,
      voter_token TEXT NOT NULL,
      PRIMARY KEY (post_id, voter_token)
    );

[ ] ADD preview.jpg (1200x630px) to WEB folder
    - Shows as thumbnail when you share your link on Discord etc.

[ ] TEST CATBOX.MOE from your location
    - Try uploading a file via the admin Add Art panel
    - If it lags, we can switch to imgbb or Cloudinary

=============================================
  FILES CHANGED (this session)
=============================================

  index.html      - Polls chip, admin Poll tab, Create Poll form
  app.js          - buildPollHtml, buildRepliesHtml, renderWall,
                    addPollOption, submitPoll, votePoll,
                    postReply, deleteReply, toggleReplyInput,
                    handleImgurUpload → switched to catbox.moe
  style.css       - Poll card, poll bars, reply thread styles
  db.js           - wall_replies + poll_votes cache & DB functions
  keys.js         - removed IMGUR_CLIENT_ID (no longer needed)

=============================================
  KEYS & CREDENTIALS LOCATION
=============================================

  keys.js         - Supabase URL + anon key (GITIGNORED, safe)
  .gitignore      - keys.js is listed here, never committed
  Supabase admin  - https://supabase.com/dashboard
  Vercel admin    - https://vercel.com/dashboard
  Live site       - https://bubz-artwork.vercel.app/

=============================================
  SUPABASE ADMIN UUID
=============================================

  Your admin UUID: 63d0c758-2da2-485c-9bbb-1892b61dbf16
  (used in RLS policies — do not lose this)

=============================================
